<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
  <meta charset="UTF-8">
  <title><%= app_name %> - Rails Mermaid ERD</title>
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" href="data:image/svg+xml;base64,<%= Base64.encode64(logo) %>">
  <style>
    #mermaid-erd {
      fill: #9c9c9c !important;
    }
  </style>
</head>
<body>
  <div id="app" class="h-screen flex flex-col">
    <header class="bg-red-600">
      <nav class="mx-auto px-4">
        <div class="flex w-full items-center justify-between py-4">
          <h1 class="text-white inline-flex space-x-1 items-center">
            <%= logo %>
            <span class="font-bold">Rails Mermaid ERD</span>
          </h1>
          <div class="space-x-4 inline-flex items-center">
            <select v-model="language" @change="setLanguage(language)" class="text-xs border-0 rounded py-1 pl-3 pr-8 focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-red-600">
              <option v-for="loc in locales" :key="loc.code" :value="loc.code">{{ loc.label }}</option>
            </select>
            <a href="https://github.com/koedame/rails-mermaid_erd" class="text-white hover:text-red-200" target="_blank" rel="noopener noreferrer">
              <svg class="h-6 w-6" fill="currentColor" viewBox="0 0 24 24">
                <path
                  fillRule="evenodd"
                  d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.531 1.032 1.531 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0112 6.844c.85.004 1.705.115 2.504.337 1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.943.359.309.678.92.678 1.855 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.019 10.019 0 0022 12.017C22 6.484 17.522 2 12 2z"
                  clipRule="evenodd"
                />
              </svg>
            </a>
          </div>
        </div>
      </nav>
    </header>

    <div class="flex flex-1 min-h-0">
      <aside ref="sidebar" @scroll.passive="onSidebarScroll" :style="{ scrollPaddingTop: `${modelListHeaderHeight}px` }" class="w-[250px] border-r rtl:border-r-0 rtl:border-l border-gray-300 flex-none p-2 overflow-y-auto [overflow-anchor:none]">
        <div class="p-2">
          <h2 class="font-medium text-gray-900 mb-1">{{i18n[language]["actions"]["title"]}}</h2>

          <div class="space-y-2">
            <button @click="reset" class="text-xs border border-gray-300 rounded py-1 w-full hover:bg-gray-100 flex justify-center items-center focus:ring focus:ring-red-600 focus:ring-offset-2">
              <svg class="w-4 h-4 mr-2" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
                <path stroke-linecap="round" stroke-linejoin="round" d="M9 15L3 9m0 0l6-6M3 9h12a6 6 0 010 12h-3" />
              </svg>
              <span>{{i18n[language]["actions"]["reset"]}}</span>
            </button>

            <button @click="onCopyUrl" class="text-xs border border-gray-300 rounded py-1 w-full hover:bg-gray-100 flex justify-center items-center focus:ring focus:ring-red-600 focus:ring-offset-2">
              <template v-if="isCopiedUrl">
                <svg class="w-4 h-4 mr-2" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
                </svg>
                <span>{{i18n[language]["actions"]["copied_url"]}}</span>
              </template>
              <template v-else>
                <svg class="w-4 h-4 mr-2" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M9 12h3.75M9 15h3.75M9 18h3.75m3 .75H18a2.25 2.25 0 002.25-2.25V6.108c0-1.135-.845-2.098-1.976-2.192a48.424 48.424 0 00-1.123-.08m-5.801 0c-.065.21-.1.433-.1.664 0 .414.336.75.75.75h4.5a.75.75 0 00.75-.75 2.25 2.25 0 00-.1-.664m-5.8 0A2.251 2.251 0 0113.5 2.25H15c1.012 0 1.867.668 2.15 1.586m-5.8 0c-.376.023-.75.05-1.124.08C9.095 4.01 8.25 4.973 8.25 6.108V8.25m0 0H4.875c-.621 0-1.125.504-1.125 1.125v11.25c0 .621.504 1.125 1.125 1.125h9.75c.621 0 1.125-.504 1.125-1.125V9.375c0-.621-.504-1.125-1.125-1.125H8.25zM6.75 12h.008v.008H6.75V12zm0 3h.008v.008H6.75V15zm0 3h.008v.008H6.75V18z" />
                </svg>
                <span>{{i18n[language]["actions"]["copy_url"]}}</span>
              </template>
            </button>

            <button @click="onCopyMermaid" class="text-xs border border-gray-300 rounded py-1 w-full hover:bg-gray-100 flex justify-center items-center focus:ring focus:ring-red-600 focus:ring-offset-2">
              <template v-if="isCopiedMermaid">
                <svg class="w-4 h-4 mr-2" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
                </svg>
                <span>{{i18n[language]["actions"]["copied_mermaid_code"]}}</span>
              </template>
              <template v-else>
                <svg class="w-4 h-4 mr-2" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M9 12h3.75M9 15h3.75M9 18h3.75m3 .75H18a2.25 2.25 0 002.25-2.25V6.108c0-1.135-.845-2.098-1.976-2.192a48.424 48.424 0 00-1.123-.08m-5.801 0c-.065.21-.1.433-.1.664 0 .414.336.75.75.75h4.5a.75.75 0 00.75-.75 2.25 2.25 0 00-.1-.664m-5.8 0A2.251 2.251 0 0113.5 2.25H15c1.012 0 1.867.668 2.15 1.586m-5.8 0c-.376.023-.75.05-1.124.08C9.095 4.01 8.25 4.973 8.25 6.108V8.25m0 0H4.875c-.621 0-1.125.504-1.125 1.125v11.25c0 .621.504 1.125 1.125 1.125h9.75c.621 0 1.125-.504 1.125-1.125V9.375c0-.621-.504-1.125-1.125-1.125H8.25zM6.75 12h.008v.008H6.75V12zm0 3h.008v.008H6.75V15zm0 3h.008v.008H6.75V18z" />
                </svg>
                <span>{{i18n[language]["actions"]["copy_mermaid_code"]}}</span>
              </template>
            </button>

            <button @click="onCopyMarkdown" class="text-xs border border-gray-300 rounded py-1 w-full hover:bg-gray-100 flex justify-center items-center focus:ring focus:ring-red-600 focus:ring-offset-2">
              <template v-if="isCopiedMarkdown">
                <svg class="w-4 h-4 mr-2" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
                </svg>
                <span>{{i18n[language]["actions"]["copied_markdown_code"]}}</span>
              </template>
              <template v-else>
                <svg class="w-4 h-4 mr-2" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M9 12h3.75M9 15h3.75M9 18h3.75m3 .75H18a2.25 2.25 0 002.25-2.25V6.108c0-1.135-.845-2.098-1.976-2.192a48.424 48.424 0 00-1.123-.08m-5.801 0c-.065.21-.1.433-.1.664 0 .414.336.75.75.75h4.5a.75.75 0 00.75-.75 2.25 2.25 0 00-.1-.664m-5.8 0A2.251 2.251 0 0113.5 2.25H15c1.012 0 1.867.668 2.15 1.586m-5.8 0c-.376.023-.75.05-1.124.08C9.095 4.01 8.25 4.973 8.25 6.108V8.25m0 0H4.875c-.621 0-1.125.504-1.125 1.125v11.25c0 .621.504 1.125 1.125 1.125h9.75c.621 0 1.125-.504 1.125-1.125V9.375c0-.621-.504-1.125-1.125-1.125H8.25zM6.75 12h.008v.008H6.75V12zm0 3h.008v.008H6.75V15zm0 3h.008v.008H6.75V18z" />
                </svg>
                <span>{{i18n[language]["actions"]["copy_markdown_code"]}}</span>
              </template>
            </button>

            <button @click="onDownloadSvg" class="text-xs border border-gray-300 rounded py-1 w-full hover:bg-gray-100 flex justify-center items-center focus:ring focus:ring-red-600 focus:ring-offset-2">
              <svg class="w-4 h-4 mr-2" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
                <path stroke-linecap="round" stroke-linejoin="round" d="M3 16.5v2.25A2.25 2.25 0 005.25 21h13.5A2.25 2.25 0 0021 18.75V16.5M16.5 12L12 16.5m0 0L7.5 12m4.5 4.5V3" />
              </svg>
              <span>{{i18n[language]["actions"]["download_svg_file"]}}</span>
            </button>

            <button @click="onDownloadPng" class="text-xs border border-gray-300 rounded py-1 w-full hover:bg-gray-100 flex justify-center items-center focus:ring focus:ring-red-600 focus:ring-offset-2">
              <svg class="w-4 h-4 mr-2" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
                <path stroke-linecap="round" stroke-linejoin="round" d="M3 16.5v2.25A2.25 2.25 0 005.25 21h13.5A2.25 2.25 0 0021 18.75V16.5M16.5 12L12 16.5m0 0L7.5 12m4.5 4.5V3" />
              </svg>
              <span>{{i18n[language]["actions"]["download_png_file"]}}</span>
            </button>
          </div>
        </div>

        <div class="p-2">
          <h2 class="font-medium text-gray-900 mb-1">{{i18n[language]["options"]["title"]}}</h2>

          <div class="space-y-1">
            <div class="min-w-0 flex-1 text-sm">
              <label class="relative flex items-start cursor-pointer hover:bg-gray-100 rounded">
                <input
                  type="checkbox"
                  class="h-4 w-4 rounded border-gray-300 text-red-600 mr-2 rtl:mr-0 rtl:ml-2 focus:ring-red-600"
                  v-model="isPreviewRelations"
                  @change="updateHash"
                />
                <span class="text-xs text-gray-900">{{i18n[language]["options"]["preview_relationships"]}}</span>
              </label>
            </div>

            <div class="min-w-0 flex-1 text-sm">
              <label class="relative flex items-start cursor-pointer hover:bg-gray-100 rounded">
                <input
                  type="checkbox"
                  class="h-4 w-4 rounded border-gray-300 text-red-600 mr-2 rtl:mr-0 rtl:ml-2 focus:ring-red-600"
                  v-model="isShowRelationComment"
                  @change="updateHash"
                />
                <span class="text-xs text-gray-900">{{i18n[language]["options"]["show_relationship_comment"]}}</span>
              </label>
            </div>

            <fieldset class="min-w-0 flex-1 text-sm">
              <legend class="text-xs text-gray-900">{{i18n[language]["options"]["columns"]}}</legend>
              <div class="space-y-1 mt-1 ml-6 rtl:ml-0 rtl:mr-6">
                <label v-for="mode in COLUMN_MODES" :key="mode" class="relative flex items-start cursor-pointer hover:bg-gray-100 rounded">
                  <input
                    type="radio"
                    name="columns"
                    class="h-4 w-4 border-gray-300 text-red-600 mr-2 rtl:mr-0 rtl:ml-2 focus:ring-red-600"
                    :value="mode"
                    v-model="columnMode"
                    @change="updateHash"
                  />
                  <span class="text-xs text-gray-900">{{i18n[language]["options"][`columns_${mode}`]}}</span>
                </label>
              </div>
            </fieldset>

            <div class="min-w-0 flex-1 text-sm">
              <label :class="`relative flex items-start hover:bg-gray-100 rounded ${ columnMode === 'none' ? 'cursor-not-allowed' : 'cursor-pointer' }`">
                <input
                  type="checkbox"
                  :class="`h-4 w-4 rounded border-gray-300 mr-2 rtl:mr-0 rtl:ml-2 focus:ring-red-600 ${ columnMode === 'none' ? 'text-gray-400' : 'text-red-600'}`"
                  v-model="isShowKey"
                  @change="updateHash"
                  :disabled="columnMode === 'none'"
                />
                <span :class="`text-xs ${ columnMode === 'none' ? 'text-gray-400' : 'text-gray-900'}`">{{i18n[language]["options"]["show_key"]}}</span>
              </label>
            </div>

            <div class="min-w-0 flex-1 text-sm">
              <label :class="`relative flex items-start hover:bg-gray-100 rounded ${ columnMode === 'none' ? 'cursor-not-allowed' : 'cursor-pointer' }`">
                <input
                  type="checkbox"
                  :class="`h-4 w-4 rounded border-gray-300 mr-2 rtl:mr-0 rtl:ml-2 focus:ring-red-600 ${ columnMode === 'none' ? 'text-gray-400' : 'text-red-600'}`"
                  v-model="isShowComment"
                  @change="updateHash"
                  :disabled="columnMode === 'none'"
                />
                <span :class="`text-xs ${ columnMode === 'none' ? 'text-gray-400' : 'text-gray-900'}`">{{i18n[language]["options"]["show_column_comment"]}}</span>
              </label>
            </div>

            <div class="min-w-0 flex-1 text-sm">
              <label class="relative flex items-start cursor-pointer hover:bg-gray-100 rounded">
                <input
                  type="checkbox"
                  class="h-4 w-4 rounded border-gray-300 text-red-600 mr-2 rtl:mr-0 rtl:ml-2 focus:ring-red-600"
                  v-model="isShowTableComment"
                  @change="updateHash"
                />
                <span class="text-xs text-gray-900">{{i18n[language]["options"]["show_table_comment"]}}</span>
              </label>
            </div>

            <div class="min-w-0 flex-1 text-sm">
              <label class="relative flex items-start cursor-pointer hover:bg-gray-100 rounded" :title="i18n[language]['options']['snapshot_mode_hint']">
                <input
                  type="checkbox"
                  class="h-4 w-4 rounded border-gray-300 text-red-600 mr-2 rtl:mr-0 rtl:ml-2 focus:ring-red-600"
                  v-model="isSnapshotMode"
                  @change="updateHash"
                />
                <span class="text-xs text-gray-900">{{i18n[language]["options"]["snapshot_mode"]}}</span>
              </label>
            </div>
          </div>
        </div>

        <!--
          The model list scrolls with the rest of the sidebar rather than in a
          box of its own, and the header above it sticks to the top so the
          filter stays in reach while browsing a long list. `-top-2` and
          `-mx-2 px-2` stretch the pinned header over the padding around it, so
          no row shows above or beside it, and `z-10` keeps the rows
          (positioned labels) from painting over it. The sidebar's
          scroll-padding-top matches the header height, so a checkbox reached
          with the keyboard is scrolled out from under the header.
        -->
        <div class="px-2 pb-2">
          <div ref="modelListHeader" class="sticky -top-2 z-10 -mx-2 px-2 bg-white pt-2">
            <h2 class="font-medium text-gray-900 mb-1">{{i18n[language]["models"]["title"]}}</h2>

            <div>
              <input type="search" class="rounded text-xs w-full py-1 px-2 border border-gray-300 bg-gray-100 focus:border-red-600 focus:ring-red-600" :placeholder="i18n[language]['models']['filter']" v-model="filterText">
            </div>

            <div class="space-x-1 text-xs text-gray-900 my-1">
              <span>{{i18n[language]["models"]["select"]}}</span>
              <button @click="onSelectAll" class="text-red-600 font-bold hover:text-red-900 rounded focus:ring-2 focus:ring-red-600 focus:ring-offset-1">{{i18n[language]["models"]["all"]}}</button>
              <span>/</span>
              <button @click="onUnselectAll" class="text-red-600 font-bold hover:text-red-900 rounded focus:ring-2 focus:ring-red-600 focus:ring-offset-1">{{i18n[language]["models"]["none"]}}</button>
            </div>

            <div class="inline-flex items-center text-xs text-gray-700 mb-1" v-if="isExistsNoModelTable">
              <svg class="text-orange-900 mr-1 h-3 w-3" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
                <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v3.75m-9.303 3.376c-.866 1.5.217 3.374 1.948 3.374h14.71c1.73 0 2.813-1.874 1.948-3.374L13.949 3.378c-.866-1.5-3.032-1.5-3.898 0L2.697 16.126zM12 15.75h.007v.008H12v-.008z" />
              </svg>
              <span>{{i18n[language]["models"]["no_model_file"]}}</span>
            </div>
          </div>

          <div ref="virtualList" class="model-list">
            <div :style="virtualListTopSpacerStyle"></div>

            <label
              v-for="model in virtualListVisibleModels"
              :key="model.ModelName"
              class="relative flex items-center cursor-pointer hover:bg-gray-100 rounded"
              :style="{ height: `${VIRTUAL_LIST_ITEM_HEIGHT_PX}px` }"
            >
              <div class="min-w-0 flex-1 text-xs text-gray-900 inline-flex items-center">
                <input
                  type="checkbox"
                  class="h-4 w-4 rounded border-gray-300 text-red-600 mr-2 rtl:mr-0 rtl:ml-2 focus:ring-red-600 shrink-0"
                  :value="model.ModelName"
                  :checked="selectModels.includes(model.ModelName)"
                  @change="onChange"
                />
                <svg v-if="!model.IsModelExist" class="text-orange-900 mr-1 h-3 w-3 shrink-0" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v3.75m-9.303 3.376c-.866 1.5.217 3.374 1.948 3.374h14.71c1.73 0 2.813-1.874 1.948-3.374L13.949 3.378c-.866-1.5-3.032-1.5-3.898 0L2.697 16.126zM12 15.75h.007v.008H12v-.008z" />
                </svg>
                <span class="truncate">{{model.ModelName}}</span>
              </div>
            </label>

            <div :style="virtualListBottomSpacerStyle"></div>
          </div>
        </div>
      </aside>

      <main class="flex-1 min-w-0 bg-gray-900 flex flex-col">
        <div class="space-x-2 inline-flex p-4">
          <button @click="tab = 'erd'" :class="`text-xs py-1 px-2 rounded hover:bg-white focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-gray-900 ${tab === 'erd' ? 'bg-white text-gray-900' : 'bg-gray-400 text-gray-900'}`">{{i18n[language]["tab"]["erd"]}}</button>
          <button @click="tab = 'code'" :class="`text-xs py-1 px-2 rounded hover:bg-white focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-gray-900 ${tab === 'code' ? 'bg-white text-gray-900' : 'bg-gray-400 text-gray-900'}`">{{i18n[language]["tab"]["code"]}}</button>
        </div>
        <div v-show="tab === 'erd'" ref="viewport" class="px-4 w-full flex-1 min-h-0 relative overflow-hidden">
          <div class="absolute inset-0" :style="zoomStyle" ref="zoomArea">
            <div v-show="!isSnapshotMode || !snapshotDataUrl" id="preview" :class="{ 'cursor-grab': isSpacePressed, 'cursor-grabbing': isDragging }"></div>
            <img v-show="isSnapshotMode && snapshotDataUrl" :src="snapshotDataUrl" :class="{ 'cursor-grab': isSpacePressed, 'cursor-grabbing': isDragging }" alt="ERD snapshot" draggable="false" style="user-select: none; -webkit-user-drag: none;" />
          </div>

          <div v-if="isEmptySelection" class="absolute inset-0 flex items-center justify-center pointer-events-none">
            <div class="bg-gray-800/80 text-white text-sm px-4 py-3 rounded inline-flex flex-col items-center space-y-1 pointer-events-auto">
              <span class="font-medium">{{i18n[language]["empty"]["title"]}}</span>
              <span class="text-white/70 text-xs">{{i18n[language]["empty"]["hint"]}}</span>
            </div>
          </div>

          <div v-if="renderError" class="absolute top-0 left-0 right-0 p-4 pointer-events-none">
            <div class="bg-red-900/90 text-white text-xs px-3 py-2 rounded inline-flex items-start space-x-2 pointer-events-auto" role="alert">
              <svg class="w-4 h-4 mt-0.5 shrink-0" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v3.75m-9.303 3.376c-.866 1.5.217 3.374 1.948 3.374h14.71c1.73 0 2.813-1.874 1.948-3.374L13.949 3.378c-.866-1.5-3.032-1.5-3.898 0L2.697 16.126zM12 15.75h.007v.008H12v-.008z" />
              </svg>
              <span>{{renderError}}</span>
              <button @click="renderError = ''" class="text-white/80 hover:text-white" :aria-label="i18n[language]['errors']['dismiss']">✕</button>
            </div>
          </div>

          <div class="absolute bottom-0 left-1/2 -translate-x-1/2 p-4">
            <div class="bg-gray-800 text-white text-[10px] px-3 py-1.5 rounded flex flex-col space-y-0.5">
              <div>
                <span class="font-medium">{{i18n[language]["controls"]["movement"]}}:</span>
                <span class="ml-1 text-white/60">{{i18n[language]["controls"]["scroll"]}} / {{i18n[language]["controls"]["arrow_keys"]}} / {{i18n[language]["controls"]["space_drag"]}} / {{i18n[language]["controls"]["middle_drag"]}}</span>
              </div>
              <div>
                <span class="font-medium">{{i18n[language]["controls"]["zoom"]}}:</span>
                <span class="ml-1 text-white/60">{{i18n[language]["controls"]["ctrl_scroll"]}} / {{i18n[language]["controls"]["pinch"]}} / {{i18n[language]["controls"]["zoom_keys"]}}</span>
              </div>
            </div>
          </div>

          <div class="absolute bottom-0 right-0 p-4 space-x-4 flex">
            <div class="space-x-2 flex items-center">
              <button class="text-xs py-1 px-2 rounded hover:bg-white focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-gray-900 bg-gray-400 text-gray-900" @click="zoomIn">+</button>
              <button class="text-xs py-1 px-2 rounded hover:bg-white focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-gray-900 bg-gray-400 text-gray-900" @click="zoomOut">-</button>
              <button :title="`${i18n[language]['controls']['reset_view']} (0)`" :aria-label="i18n[language]['controls']['reset_view']" class="text-xs py-1 px-2 rounded hover:bg-white focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-gray-900 bg-gray-400 text-gray-900" @click="resetView">⌂</button>
            </div>
            <div class="flex items-center space-x-2">
              <button class="text-xs py-1 px-2 rounded hover:bg-white focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-gray-900 bg-gray-400 text-gray-900" @click="moveLeft">←</button>
              <div class="flex flex-col space-y-8">
                <button class="text-xs py-1 px-2 rounded hover:bg-white focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-gray-900 bg-gray-400 text-gray-900" @click="moveUp">↑</button>
                <button class="text-xs py-1 px-2 rounded hover:bg-white focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-gray-900 bg-gray-400 text-gray-900" @click="moveDown">↓</button>
              </div>
              <button class="text-xs py-1 px-2 rounded hover:bg-white focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-gray-900 bg-gray-400 text-gray-900" @click="moveRight">→</button>
            </div>
          </div>
        </div>
        <textarea v-show="tab === 'code'" class="px-4 bg-gray-900 text-gray-300 font-mono w-full text-xs flex-1 min-h-0 border-0 focus:ring-0" readonly v-model="mermaidErd"></textarea>
      </main>
    </div>

    <footer class="bg-gray-100">
      <p class="text-center text-xs text-gray-600 py-2">
        <a href="https://github.com/koedame/rails-mermaid_erd" class="hover:text-gray-400" target="_blank" rel="noopener noreferrer">
          Rails Mermaid ERD v<%= version %>
        </a>
      </p>
    </footer>
  </div>

  <script><%= tailwindcss_js %></script>
  <script><%= mermaid_js %></script>
  <script><%= vue_js %></script>

  <script>window.SCHEMA_DATA=<%= result.to_json %></script>
  <script>window.VIEWER_DEFAULTS=<%= viewer_defaults.to_json %></script>
  <script>
    window.locales = [
      { code: 'en', label: 'English', dir: 'ltr' },
      { code: 'ja', label: '日本語', dir: 'ltr' },
      { code: 'zh-CN', label: '简体中文', dir: 'ltr' },
      { code: 'zh-TW', label: '繁體中文', dir: 'ltr' },
      { code: 'ko', label: '한국어', dir: 'ltr' },
      { code: 'es', label: 'Español', dir: 'ltr' },
      { code: 'fr', label: 'Français', dir: 'ltr' },
      { code: 'de', label: 'Deutsch', dir: 'ltr' },
      { code: 'it', label: 'Italiano', dir: 'ltr' },
      { code: 'pt-BR', label: 'Português (Brasil)', dir: 'ltr' },
      { code: 'ru', label: 'Русский', dir: 'ltr' },
      { code: 'ar', label: 'العربية', dir: 'rtl' },
    ]
    window.i18n = {
      en: {
        actions: {
          title: 'Actions',
          reset: 'Reset',
          copy_mermaid_code: 'Copy Mermaid Code',
          copied_mermaid_code: 'Copied Mermaid Code',
          copy_markdown_code: 'Copy Markdown Code',
          copied_markdown_code: 'Copied Markdown Code',
          copy_url: 'Copy Link for Sharing',
          copied_url: 'Copied Link for Sharing',
          download_svg_file: 'Download SVG File',
          download_png_file: 'Download PNG File',
        },
        options: {
          title: 'Options',
          preview_relationships: 'Preview Relationships',
          show_relationship_comment: 'Show Relationship Comment',
          show_key: 'Show Key',
          show_column_comment: 'Show Column Comment',
          show_table_comment: 'Show Table Comment',
          columns: 'Columns',
          columns_all: 'All',
          columns_keys: 'Keys Only',
          columns_none: 'None',
          snapshot_mode: 'Snapshot Mode',
          snapshot_mode_hint: 'Rasterise the diagram after each render. Pan and zoom become much faster on Safari and Firefox at the cost of text selection.',
        },
        models: {
          title: 'Models',
          select: 'Select',
          all: 'All',
          none: 'None',
          filter: 'Filter',
          no_model_file: 'No Model File',
        },
        empty: {
          title: 'No models selected',
          hint: 'Pick models from the sidebar to render the ERD.',
        },
        errors: {
          render_failed: 'Mermaid failed to render the current selection. Check the browser console for details.',
          snapshot_failed: 'Could not bake the snapshot image. Falling back to the SVG render.',
          download_failed: 'Download failed. Check the browser console for details.',
          copy_failed: 'Copy to clipboard failed. Browsers require a recent user gesture and a secure context.',
          dismiss: 'Dismiss',
        },
        tab: {
          erd: 'ERD',
          code: 'Code',
        },
        controls: {
          movement: 'Movement',
          zoom: 'Zoom',
          space_drag: 'Space + Mouse Drag',
          middle_drag: 'Middle Click + Drag',
          scroll: 'Scroll',
          arrow_keys: 'Arrow Keys',
          ctrl_scroll: 'Ctrl/⌘ + Scroll',
          zoom_keys: '+/- Keys',
          pinch: 'Pinch In/Out',
          reset_view: 'Reset View',
        }
      },
      ja: {
        actions: {
          title: '操作',
          reset: 'リセット',
          copy_mermaid_code: 'Mermaid コードをコピー',
          copied_mermaid_code: 'コピーしました',
          copy_markdown_code: 'Markdown コードをコピー',
          copied_markdown_code: 'コピーしました',
          copy_url: '共有リンクをコピー',
          copied_url: 'コピーしました',
          download_svg_file: 'SVG ファイルをダウンロード',
          download_png_file: 'PNG ファイルをダウンロード',
        },
        options: {
          title: '設定',
          preview_relationships: 'リレーションをプレビュー',
          show_relationship_comment: 'リレーションのコメントを表示',
          show_key: 'キーを表示',
          show_column_comment: 'コメントを表示',
          show_table_comment: 'テーブルコメントを表示',
          columns: 'カラム',
          columns_all: 'すべて',
          columns_keys: 'キーのみ',
          columns_none: 'なし',
          snapshot_mode: 'スナップショットモード',
          snapshot_mode_hint: '描画後に図を画像化します。Safari / Firefox での拡大縮小やパン操作が高速になりますが、テキスト選択はできなくなります。',
        },
        models: {
          title: 'モデル',
          select: '選択',
          all: '全て',
          none: 'なし',
          filter: '絞り込み',
          no_model_file: 'モデル未定義',
        },
        empty: {
          title: 'モデルが選択されていません',
          hint: 'サイドバーからモデルを選択して ER 図を描画します。',
        },
        errors: {
          render_failed: 'Mermaid の描画に失敗しました。詳細はブラウザのコンソールを確認してください。',
          snapshot_failed: 'スナップショット画像の生成に失敗しました。SVG 表示に戻します。',
          download_failed: 'ダウンロードに失敗しました。詳細はブラウザのコンソールを確認してください。',
          copy_failed: 'クリップボードへのコピーに失敗しました。ブラウザはユーザー操作直後かつセキュアな環境を要求します。',
          dismiss: '閉じる',
        },
        tab: {
          erd: 'ER図',
          code: 'コード',
        },
        controls: {
          movement: '移動方法',
          zoom: '拡大/縮小',
          space_drag: 'スペースキー + マウスドラッグ',
          middle_drag: '中クリック + ドラッグ',
          scroll: 'スクロール',
          arrow_keys: '矢印キー',
          ctrl_scroll: 'Ctrl/⌘ + スクロール',
          zoom_keys: '+/- キー',
          pinch: 'ピンチイン/アウト',
          reset_view: 'ビューをリセット',
        }
      },
      'zh-CN': {
        actions: {
          title: '操作',
          reset: '重置',
          copy_mermaid_code: '复制 Mermaid 代码',
          copied_mermaid_code: '已复制 Mermaid 代码',
          copy_markdown_code: '复制 Markdown 代码',
          copied_markdown_code: '已复制 Markdown 代码',
          copy_url: '复制分享链接',
          copied_url: '已复制分享链接',
          download_svg_file: '下载 SVG 文件',
          download_png_file: '下载 PNG 文件',
        },
        options: {
          title: '选项',
          preview_relationships: '预览关系',
          show_relationship_comment: '显示关系注释',
          show_key: '显示键',
          show_column_comment: '显示字段注释',
          show_table_comment: '显示表注释',
          columns: '字段',
          columns_all: '全部',
          columns_keys: '仅显示键',
          columns_none: '无',
          snapshot_mode: '快照模式',
          snapshot_mode_hint: '每次渲染后将图表栅格化。在 Safari 和 Firefox 上平移和缩放会快很多，但代价是无法选择文本。',
        },
        models: {
          title: '模型',
          select: '选择',
          all: '全部',
          none: '无',
          filter: '筛选',
          no_model_file: '无模型文件',
        },
        empty: {
          title: '未选择模型',
          hint: '从侧边栏选择模型以渲染 ERD。',
        },
        errors: {
          render_failed: 'Mermaid 渲染当前选择失败。请查看浏览器控制台了解详情。',
          snapshot_failed: '无法生成快照图像。回退到 SVG 渲染。',
          download_failed: '下载失败。请查看浏览器控制台了解详情。',
          copy_failed: '复制到剪贴板失败。浏览器要求近期的用户操作以及安全上下文。',
          dismiss: '关闭',
        },
        tab: {
          erd: 'ER 图',
          code: '代码',
        },
        controls: {
          movement: '移动',
          zoom: '缩放',
          space_drag: '空格 + 鼠标拖动',
          middle_drag: '中键 + 拖动',
          scroll: '滚动',
          arrow_keys: '方向键',
          ctrl_scroll: 'Ctrl/⌘ + 滚动',
          zoom_keys: '+/- 键',
          pinch: '双指缩放',
          reset_view: '重置视图',
        }
      },
      'zh-TW': {
        actions: {
          title: '操作',
          reset: '重設',
          copy_mermaid_code: '複製 Mermaid 程式碼',
          copied_mermaid_code: '已複製 Mermaid 程式碼',
          copy_markdown_code: '複製 Markdown 程式碼',
          copied_markdown_code: '已複製 Markdown 程式碼',
          copy_url: '複製分享連結',
          copied_url: '已複製分享連結',
          download_svg_file: '下載 SVG 檔案',
          download_png_file: '下載 PNG 檔案',
        },
        options: {
          title: '選項',
          preview_relationships: '預覽關聯',
          show_relationship_comment: '顯示關聯註解',
          show_key: '顯示鍵',
          show_column_comment: '顯示欄位註解',
          show_table_comment: '顯示資料表註解',
          columns: '欄位',
          columns_all: '全部',
          columns_keys: '僅顯示鍵',
          columns_none: '無',
          snapshot_mode: '快照模式',
          snapshot_mode_hint: '每次算繪後將圖表點陣化。在 Safari 與 Firefox 上平移與縮放會快很多，但代價是無法選取文字。',
        },
        models: {
          title: '模型',
          select: '選擇',
          all: '全部',
          none: '無',
          filter: '篩選',
          no_model_file: '無模型檔案',
        },
        empty: {
          title: '尚未選擇模型',
          hint: '從側邊欄選擇模型以算繪 ERD。',
        },
        errors: {
          render_failed: 'Mermaid 算繪目前的選擇失敗。請查看瀏覽器主控台以瞭解詳情。',
          snapshot_failed: '無法產生快照影像。回復為 SVG 算繪。',
          download_failed: '下載失敗。請查看瀏覽器主控台以瞭解詳情。',
          copy_failed: '複製到剪貼簿失敗。瀏覽器需要近期的使用者操作以及安全的內容環境。',
          dismiss: '關閉',
        },
        tab: {
          erd: 'ER 圖',
          code: '程式碼',
        },
        controls: {
          movement: '移動',
          zoom: '縮放',
          space_drag: '空白鍵 + 滑鼠拖曳',
          middle_drag: '中鍵 + 拖曳',
          scroll: '捲動',
          arrow_keys: '方向鍵',
          ctrl_scroll: 'Ctrl/⌘ + 捲動',
          zoom_keys: '+/- 鍵',
          pinch: '雙指縮放',
          reset_view: '重設檢視',
        }
      },
      ko: {
        actions: {
          title: '작업',
          reset: '초기화',
          copy_mermaid_code: 'Mermaid 코드 복사',
          copied_mermaid_code: 'Mermaid 코드 복사됨',
          copy_markdown_code: 'Markdown 코드 복사',
          copied_markdown_code: 'Markdown 코드 복사됨',
          copy_url: '공유 링크 복사',
          copied_url: '공유 링크 복사됨',
          download_svg_file: 'SVG 파일 다운로드',
          download_png_file: 'PNG 파일 다운로드',
        },
        options: {
          title: '옵션',
          preview_relationships: '관계 미리보기',
          show_relationship_comment: '관계 주석 표시',
          show_key: '키 표시',
          show_column_comment: '컬럼 주석 표시',
          show_table_comment: '테이블 주석 표시',
          columns: '컬럼',
          columns_all: '전체',
          columns_keys: '키만',
          columns_none: '없음',
          snapshot_mode: '스냅샷 모드',
          snapshot_mode_hint: '렌더링할 때마다 다이어그램을 래스터화합니다. Safari와 Firefox에서 이동과 확대/축소가 훨씬 빨라지지만 텍스트를 선택할 수 없게 됩니다.',
        },
        models: {
          title: '모델',
          select: '선택',
          all: '전체',
          none: '없음',
          filter: '필터',
          no_model_file: '모델 파일 없음',
        },
        empty: {
          title: '선택된 모델이 없습니다',
          hint: '사이드바에서 모델을 선택하여 ERD를 렌더링하세요.',
        },
        errors: {
          render_failed: 'Mermaid가 현재 선택을 렌더링하지 못했습니다. 자세한 내용은 브라우저 콘솔을 확인하세요.',
          snapshot_failed: '스냅샷 이미지를 생성하지 못했습니다. SVG 렌더링으로 대체합니다.',
          download_failed: '다운로드에 실패했습니다. 자세한 내용은 브라우저 콘솔을 확인하세요.',
          copy_failed: '클립보드 복사에 실패했습니다. 브라우저는 최근의 사용자 동작과 보안 컨텍스트를 요구합니다.',
          dismiss: '닫기',
        },
        tab: {
          erd: 'ERD',
          code: '코드',
        },
        controls: {
          movement: '이동',
          zoom: '확대/축소',
          space_drag: '스페이스 + 마우스 드래그',
          middle_drag: '가운데 클릭 + 드래그',
          scroll: '스크롤',
          arrow_keys: '화살표 키',
          ctrl_scroll: 'Ctrl/⌘ + 스크롤',
          zoom_keys: '+/- 키',
          pinch: '핀치 확대/축소',
          reset_view: '보기 초기화',
        }
      },
      es: {
        actions: {
          title: 'Acciones',
          reset: 'Restablecer',
          copy_mermaid_code: 'Copiar código Mermaid',
          copied_mermaid_code: 'Código Mermaid copiado',
          copy_markdown_code: 'Copiar código Markdown',
          copied_markdown_code: 'Código Markdown copiado',
          copy_url: 'Copiar enlace para compartir',
          copied_url: 'Enlace para compartir copiado',
          download_svg_file: 'Descargar archivo SVG',
          download_png_file: 'Descargar archivo PNG',
        },
        options: {
          title: 'Opciones',
          preview_relationships: 'Previsualizar relaciones',
          show_relationship_comment: 'Mostrar comentario de la relación',
          show_key: 'Mostrar clave',
          show_column_comment: 'Mostrar comentario de la columna',
          show_table_comment: 'Mostrar comentario de la tabla',
          columns: 'Columnas',
          columns_all: 'Todas',
          columns_keys: 'Solo claves',
          columns_none: 'Ninguna',
          snapshot_mode: 'Modo instantánea',
          snapshot_mode_hint: 'Rasteriza el diagrama después de cada renderizado. El desplazamiento y el zoom son mucho más rápidos en Safari y Firefox, a costa de no poder seleccionar texto.',
        },
        models: {
          title: 'Modelos',
          select: 'Seleccionar',
          all: 'Todos',
          none: 'Ninguno',
          filter: 'Filtrar',
          no_model_file: 'Sin archivo de modelo',
        },
        empty: {
          title: 'Ningún modelo seleccionado',
          hint: 'Elige modelos en la barra lateral para renderizar el ERD.',
        },
        errors: {
          render_failed: 'Mermaid no pudo renderizar la selección actual. Revisa la consola del navegador para más detalles.',
          snapshot_failed: 'No se pudo generar la imagen de la instantánea. Volviendo al renderizado SVG.',
          download_failed: 'La descarga falló. Revisa la consola del navegador para más detalles.',
          copy_failed: 'No se pudo copiar al portapapeles. Los navegadores requieren una acción reciente del usuario y un contexto seguro.',
          dismiss: 'Descartar',
        },
        tab: {
          erd: 'ERD',
          code: 'Código',
        },
        controls: {
          movement: 'Movimiento',
          zoom: 'Zoom',
          space_drag: 'Espacio + arrastrar con el ratón',
          middle_drag: 'Clic central + arrastrar',
          scroll: 'Desplazamiento',
          arrow_keys: 'Teclas de flecha',
          ctrl_scroll: 'Ctrl/⌘ + desplazamiento',
          zoom_keys: 'Teclas +/-',
          pinch: 'Pellizcar para acercar/alejar',
          reset_view: 'Restablecer vista',
        }
      },
      fr: {
        actions: {
          title: 'Actions',
          reset: 'Réinitialiser',
          copy_mermaid_code: 'Copier le code Mermaid',
          copied_mermaid_code: 'Code Mermaid copié',
          copy_markdown_code: 'Copier le code Markdown',
          copied_markdown_code: 'Code Markdown copié',
          copy_url: 'Copier le lien de partage',
          copied_url: 'Lien de partage copié',
          download_svg_file: 'Télécharger le fichier SVG',
          download_png_file: 'Télécharger le fichier PNG',
        },
        options: {
          title: 'Options',
          preview_relationships: 'Aperçu des relations',
          show_relationship_comment: 'Afficher le commentaire de la relation',
          show_key: 'Afficher la clé',
          show_column_comment: 'Afficher le commentaire de la colonne',
          show_table_comment: 'Afficher le commentaire de la table',
          columns: 'Colonnes',
          columns_all: 'Toutes',
          columns_keys: 'Clés uniquement',
          columns_none: 'Aucune',
          snapshot_mode: 'Mode instantané',
          snapshot_mode_hint: 'Pixellise le diagramme après chaque rendu. Le déplacement et le zoom deviennent beaucoup plus rapides sur Safari et Firefox, au prix de la sélection du texte.',
        },
        models: {
          title: 'Modèles',
          select: 'Sélectionner',
          all: 'Tous',
          none: 'Aucun',
          filter: 'Filtrer',
          no_model_file: 'Aucun fichier de modèle',
        },
        empty: {
          title: 'Aucun modèle sélectionné',
          hint: 'Sélectionnez des modèles dans la barre latérale pour afficher l\'ERD.',
        },
        errors: {
          render_failed: 'Mermaid n\'a pas pu afficher la sélection actuelle. Consultez la console du navigateur pour plus de détails.',
          snapshot_failed: 'Impossible de générer l\'image instantanée. Retour au rendu SVG.',
          download_failed: 'Le téléchargement a échoué. Consultez la console du navigateur pour plus de détails.',
          copy_failed: 'La copie dans le presse-papiers a échoué. Les navigateurs exigent une action utilisateur récente et un contexte sécurisé.',
          dismiss: 'Fermer',
        },
        tab: {
          erd: 'ERD',
          code: 'Code',
        },
        controls: {
          movement: 'Déplacement',
          zoom: 'Zoom',
          space_drag: 'Espace + glisser avec la souris',
          middle_drag: 'Clic du milieu + glisser',
          scroll: 'Défilement',
          arrow_keys: 'Touches fléchées',
          ctrl_scroll: 'Ctrl/⌘ + défilement',
          zoom_keys: 'Touches +/-',
          pinch: 'Pincer pour zoomer',
          reset_view: 'Réinitialiser la vue',
        }
      },
      de: {
        actions: {
          title: 'Aktionen',
          reset: 'Zurücksetzen',
          copy_mermaid_code: 'Mermaid-Code kopieren',
          copied_mermaid_code: 'Mermaid-Code kopiert',
          copy_markdown_code: 'Markdown-Code kopieren',
          copied_markdown_code: 'Markdown-Code kopiert',
          copy_url: 'Freigabelink kopieren',
          copied_url: 'Freigabelink kopiert',
          download_svg_file: 'SVG-Datei herunterladen',
          download_png_file: 'PNG-Datei herunterladen',
        },
        options: {
          title: 'Optionen',
          preview_relationships: 'Beziehungen vorschauen',
          show_relationship_comment: 'Beziehungskommentar anzeigen',
          show_key: 'Schlüssel anzeigen',
          show_column_comment: 'Spaltenkommentar anzeigen',
          show_table_comment: 'Tabellenkommentar anzeigen',
          columns: 'Spalten',
          columns_all: 'Alle',
          columns_keys: 'Nur Schlüssel',
          columns_none: 'Keine',
          snapshot_mode: 'Schnappschussmodus',
          snapshot_mode_hint: 'Rastert das Diagramm nach jedem Rendern. Verschieben und Zoomen werden in Safari und Firefox deutlich schneller, allerdings auf Kosten der Textauswahl.',
        },
        models: {
          title: 'Modelle',
          select: 'Auswählen',
          all: 'Alle',
          none: 'Keine',
          filter: 'Filtern',
          no_model_file: 'Keine Modelldatei',
        },
        empty: {
          title: 'Keine Modelle ausgewählt',
          hint: 'Wählen Sie Modelle in der Seitenleiste aus, um das ERD darzustellen.',
        },
        errors: {
          render_failed: 'Mermaid konnte die aktuelle Auswahl nicht darstellen. Details finden Sie in der Browser-Konsole.',
          snapshot_failed: 'Das Schnappschussbild konnte nicht erstellt werden. Es wird auf die SVG-Darstellung zurückgegriffen.',
          download_failed: 'Download fehlgeschlagen. Details finden Sie in der Browser-Konsole.',
          copy_failed: 'Kopieren in die Zwischenablage fehlgeschlagen. Browser erfordern eine kürzliche Nutzeraktion und einen sicheren Kontext.',
          dismiss: 'Schließen',
        },
        tab: {
          erd: 'ERD',
          code: 'Code',
        },
        controls: {
          movement: 'Bewegung',
          zoom: 'Zoom',
          space_drag: 'Leertaste + Maus ziehen',
          middle_drag: 'Mittelklick + Ziehen',
          scroll: 'Scrollen',
          arrow_keys: 'Pfeiltasten',
          ctrl_scroll: 'Strg/⌘ + Scrollen',
          zoom_keys: 'Tasten +/-',
          pinch: 'Zwei-Finger-Zoom',
          reset_view: 'Ansicht zurücksetzen',
        }
      },
      it: {
        actions: {
          title: 'Azioni',
          reset: 'Reimposta',
          copy_mermaid_code: 'Copia codice Mermaid',
          copied_mermaid_code: 'Codice Mermaid copiato',
          copy_markdown_code: 'Copia codice Markdown',
          copied_markdown_code: 'Codice Markdown copiato',
          copy_url: 'Copia link di condivisione',
          copied_url: 'Link di condivisione copiato',
          download_svg_file: 'Scarica file SVG',
          download_png_file: 'Scarica file PNG',
        },
        options: {
          title: 'Opzioni',
          preview_relationships: 'Anteprima relazioni',
          show_relationship_comment: 'Mostra commento della relazione',
          show_key: 'Mostra chiave',
          show_column_comment: 'Mostra commento della colonna',
          show_table_comment: 'Mostra commento della tabella',
          columns: 'Colonne',
          columns_all: 'Tutte',
          columns_keys: 'Solo chiavi',
          columns_none: 'Nessuna',
          snapshot_mode: 'Modalità istantanea',
          snapshot_mode_hint: 'Rasterizza il diagramma dopo ogni rendering. Lo spostamento e lo zoom diventano molto più veloci su Safari e Firefox, a scapito della selezione del testo.',
        },
        models: {
          title: 'Modelli',
          select: 'Seleziona',
          all: 'Tutti',
          none: 'Nessuno',
          filter: 'Filtra',
          no_model_file: 'Nessun file di modello',
        },
        empty: {
          title: 'Nessun modello selezionato',
          hint: 'Seleziona i modelli dalla barra laterale per visualizzare l\'ERD.',
        },
        errors: {
          render_failed: 'Mermaid non è riuscito a visualizzare la selezione corrente. Controlla la console del browser per i dettagli.',
          snapshot_failed: 'Impossibile generare l\'immagine istantanea. Ritorno al rendering SVG.',
          download_failed: 'Download non riuscito. Controlla la console del browser per i dettagli.',
          copy_failed: 'Copia negli appunti non riuscita. I browser richiedono un\'azione utente recente e un contesto sicuro.',
          dismiss: 'Chiudi',
        },
        tab: {
          erd: 'ERD',
          code: 'Codice',
        },
        controls: {
          movement: 'Movimento',
          zoom: 'Zoom',
          space_drag: 'Spazio + trascina con il mouse',
          middle_drag: 'Clic centrale + trascina',
          scroll: 'Scorrimento',
          arrow_keys: 'Tasti freccia',
          ctrl_scroll: 'Ctrl/⌘ + scorrimento',
          zoom_keys: 'Tasti +/-',
          pinch: 'Pizzica per zoomare',
          reset_view: 'Reimposta vista',
        }
      },
      'pt-BR': {
        actions: {
          title: 'Ações',
          reset: 'Redefinir',
          copy_mermaid_code: 'Copiar código Mermaid',
          copied_mermaid_code: 'Código Mermaid copiado',
          copy_markdown_code: 'Copiar código Markdown',
          copied_markdown_code: 'Código Markdown copiado',
          copy_url: 'Copiar link de compartilhamento',
          copied_url: 'Link de compartilhamento copiado',
          download_svg_file: 'Baixar arquivo SVG',
          download_png_file: 'Baixar arquivo PNG',
        },
        options: {
          title: 'Opções',
          preview_relationships: 'Pré-visualizar relacionamentos',
          show_relationship_comment: 'Mostrar comentário do relacionamento',
          show_key: 'Mostrar chave',
          show_column_comment: 'Mostrar comentário da coluna',
          show_table_comment: 'Mostrar comentário da tabela',
          columns: 'Colunas',
          columns_all: 'Todas',
          columns_keys: 'Somente chaves',
          columns_none: 'Nenhuma',
          snapshot_mode: 'Modo instantâneo',
          snapshot_mode_hint: 'Rasteriza o diagrama após cada renderização. Mover e dar zoom ficam muito mais rápidos no Safari e no Firefox, ao custo da seleção de texto.',
        },
        models: {
          title: 'Modelos',
          select: 'Selecionar',
          all: 'Todos',
          none: 'Nenhum',
          filter: 'Filtrar',
          no_model_file: 'Sem arquivo de modelo',
        },
        empty: {
          title: 'Nenhum modelo selecionado',
          hint: 'Escolha modelos na barra lateral para renderizar o ERD.',
        },
        errors: {
          render_failed: 'O Mermaid não conseguiu renderizar a seleção atual. Verifique o console do navegador para mais detalhes.',
          snapshot_failed: 'Não foi possível gerar a imagem do instantâneo. Voltando à renderização SVG.',
          download_failed: 'Falha no download. Verifique o console do navegador para mais detalhes.',
          copy_failed: 'Falha ao copiar para a área de transferência. Os navegadores exigem uma ação recente do usuário e um contexto seguro.',
          dismiss: 'Dispensar',
        },
        tab: {
          erd: 'ERD',
          code: 'Código',
        },
        controls: {
          movement: 'Movimento',
          zoom: 'Zoom',
          space_drag: 'Espaço + arrastar com o mouse',
          middle_drag: 'Clique do meio + arrastar',
          scroll: 'Rolagem',
          arrow_keys: 'Setas do teclado',
          ctrl_scroll: 'Ctrl/⌘ + rolagem',
          zoom_keys: 'Teclas +/-',
          pinch: 'Pinçar para ampliar/reduzir',
          reset_view: 'Redefinir visualização',
        }
      },
      ru: {
        actions: {
          title: 'Действия',
          reset: 'Сбросить',
          copy_mermaid_code: 'Копировать код Mermaid',
          copied_mermaid_code: 'Код Mermaid скопирован',
          copy_markdown_code: 'Копировать код Markdown',
          copied_markdown_code: 'Код Markdown скопирован',
          copy_url: 'Копировать ссылку для обмена',
          copied_url: 'Ссылка для обмена скопирована',
          download_svg_file: 'Скачать файл SVG',
          download_png_file: 'Скачать файл PNG',
        },
        options: {
          title: 'Параметры',
          preview_relationships: 'Предпросмотр связей',
          show_relationship_comment: 'Показать комментарий связи',
          show_key: 'Показать ключ',
          show_column_comment: 'Показать комментарий столбца',
          show_table_comment: 'Показать комментарий таблицы',
          columns: 'Столбцы',
          columns_all: 'Все',
          columns_keys: 'Только ключи',
          columns_none: 'Нет',
          snapshot_mode: 'Режим снимка',
          snapshot_mode_hint: 'Растрировать диаграмму после каждой отрисовки. Панорамирование и масштабирование становятся намного быстрее в Safari и Firefox, но текст нельзя будет выделить.',
        },
        models: {
          title: 'Модели',
          select: 'Выбрать',
          all: 'Все',
          none: 'Нет',
          filter: 'Фильтр',
          no_model_file: 'Нет файла модели',
        },
        empty: {
          title: 'Модели не выбраны',
          hint: 'Выберите модели на боковой панели, чтобы отобразить ERD.',
        },
        errors: {
          render_failed: 'Mermaid не удалось отобразить текущий выбор. Подробности смотрите в консоли браузера.',
          snapshot_failed: 'Не удалось создать изображение снимка. Возврат к отрисовке SVG.',
          download_failed: 'Не удалось скачать. Подробности смотрите в консоли браузера.',
          copy_failed: 'Не удалось скопировать в буфер обмена. Браузеры требуют недавнего действия пользователя и безопасного контекста.',
          dismiss: 'Закрыть',
        },
        tab: {
          erd: 'ERD',
          code: 'Код',
        },
        controls: {
          movement: 'Перемещение',
          zoom: 'Масштаб',
          space_drag: 'Пробел + перетаскивание мышью',
          middle_drag: 'Средняя кнопка + перетаскивание',
          scroll: 'Прокрутка',
          arrow_keys: 'Клавиши со стрелками',
          ctrl_scroll: 'Ctrl/⌘ + прокрутка',
          zoom_keys: 'Клавиши +/-',
          pinch: 'Щипок для масштабирования',
          reset_view: 'Сбросить вид',
        }
      },
      ar: {
        actions: {
          title: 'الإجراءات',
          reset: 'إعادة تعيين',
          copy_mermaid_code: 'نسخ كود Mermaid',
          copied_mermaid_code: 'تم نسخ كود Mermaid',
          copy_markdown_code: 'نسخ كود Markdown',
          copied_markdown_code: 'تم نسخ كود Markdown',
          copy_url: 'نسخ رابط المشاركة',
          copied_url: 'تم نسخ رابط المشاركة',
          download_svg_file: 'تنزيل ملف SVG',
          download_png_file: 'تنزيل ملف PNG',
        },
        options: {
          title: 'الخيارات',
          preview_relationships: 'معاينة العلاقات',
          show_relationship_comment: 'إظهار تعليق العلاقة',
          show_key: 'إظهار المفتاح',
          show_column_comment: 'إظهار تعليق العمود',
          show_table_comment: 'إظهار تعليق الجدول',
          columns: 'الأعمدة',
          columns_all: 'الكل',
          columns_keys: 'المفاتيح فقط',
          columns_none: 'لا شيء',
          snapshot_mode: 'وضع اللقطة',
          snapshot_mode_hint: 'تحويل المخطط إلى صورة نقطية بعد كل عرض. يصبح التحريك والتكبير أسرع بكثير في Safari وFirefox، لكن على حساب إمكانية تحديد النص.',
        },
        models: {
          title: 'النماذج',
          select: 'تحديد',
          all: 'الكل',
          none: 'لا شيء',
          filter: 'تصفية',
          no_model_file: 'لا يوجد ملف نموذج',
        },
        empty: {
          title: 'لم يتم تحديد أي نموذج',
          hint: 'اختر النماذج من الشريط الجانبي لعرض المخطط (ERD).',
        },
        errors: {
          render_failed: 'فشل Mermaid في عرض التحديد الحالي. تحقق من وحدة تحكم المتصفح لمزيد من التفاصيل.',
          snapshot_failed: 'تعذّر إنشاء صورة اللقطة. سيتم الرجوع إلى عرض SVG.',
          download_failed: 'فشل التنزيل. تحقق من وحدة تحكم المتصفح لمزيد من التفاصيل.',
          copy_failed: 'فشل النسخ إلى الحافظة. تتطلب المتصفحات إجراءً حديثًا من المستخدم وسياقًا آمنًا.',
          dismiss: 'إغلاق',
        },
        tab: {
          erd: 'ERD',
          code: 'الكود',
        },
        controls: {
          movement: 'الحركة',
          zoom: 'التكبير',
          space_drag: 'المسافة + سحب الماوس',
          middle_drag: 'النقر الأوسط + السحب',
          scroll: 'التمرير',
          arrow_keys: 'مفاتيح الأسهم',
          ctrl_scroll: 'Ctrl/⌘ + التمرير',
          zoom_keys: 'المفتاحان +/-',
          pinch: 'القرص للتكبير/التصغير',
          reset_view: 'إعادة تعيين العرض',
        }
      }
    }
  </script>
  <script>
    const App = {
      setup(){
        const language = Vue.ref('en')
        const isCopiedMermaid = Vue.ref(false)
        const isCopiedMarkdown = Vue.ref(false)
        const tab = Vue.ref('erd')

        const schemaData = {
          ...window.SCHEMA_DATA,
          Models: window.SCHEMA_DATA.Models.sort((a, b) => a.ModelName < b.ModelName ? -1 : 1)
        }
        // The view the app's config/mermaid_erd.yml asks for on a first visit and
        // after Reset: `{models: [...], columns: 'all' | 'keys' | 'none'}`.
        const viewerDefaults = window.VIEWER_DEFAULTS
        const selectModels = Vue.ref([])
        const isPreviewRelations = Vue.ref(false)
        const isShowRelationComment = Vue.ref(false)
        const isShowKey = Vue.ref(false)
        const isShowComment = Vue.ref(false)
        const isShowTableComment = Vue.ref(false)
        // Which columns each model box draws. Keys Only keeps the columns the
        // schema marks as a primary or foreign key, so a large diagram stays
        // small without dropping the columns its relations are declared on.
        const COLUMN_MODES = ['all', 'keys', 'none']
        const columnMode = Vue.ref('all')
        // Snapshot mode bakes the rendered SVG into a raster image so pan/zoom
        // transforms a single <img> instead of relaying out hundreds of <g>
        // elements. Safari and Firefox get the biggest win; Chromium handles
        // SVG transforms well enough that the default stays SVG.
        const isSnapshotMode = Vue.ref(false)
        const snapshotDataUrl = Vue.ref('')
        // Surfaced render failure: Mermaid throws on invalid input, snapshot
        // baking can fail on OOM, etc. Without a visible signal the user just
        // sees a stale (or empty) preview — so we expose it on the overlay.
        const renderError = Vue.ref('')

        const MIN_SCALE = 0.1
        const MAX_SCALE = 5
        // Ctrl + wheel delta in pixels → zoom factor. Browsers other than Safari report a trackpad
        // pinch as Ctrl + wheel events; Chromium sets deltaY = -100 * ln(pinch scale), so this
        // sensitivity makes the diagram follow the fingers.
        const WHEEL_ZOOM_SENSITIVITY = 0.01
        const BUTTON_ZOOM_FACTOR = 1.2
        // A mouse wheel notch arrives as one large delta (around 100px) that would zoom by ~2.7x.
        // Capping each event at the delta that equals one button step makes a notch zoom like the
        // + / - buttons, while pinch events (a few pixels each) stay under the cap.
        const WHEEL_ZOOM_MAX_DELTA_PX = Math.log(BUTTON_ZOOM_FACTOR) / WHEEL_ZOOM_SENSITIVITY
        const PAN_STEP_PX = 60
        // Approximate pixels-per-line for wheel events that report DOM_DELTA_LINE
        // (Firefox's default for mouse wheels, e.g. ±3 per notch).
        const WHEEL_LINE_HEIGHT_PX = 16

        const scale = Vue.ref(1)
        const posX = Vue.ref(0)
        const posY = Vue.ref(0)
        // `zoomArea` is the transformed inner element; `viewport` is its untransformed parent
        // and the only stable reference frame for cursor-anchored zoom maths.
        const zoomArea = Vue.ref(null)
        const viewport = Vue.ref(null)
        // Manage space key press state
        const isSpacePressed = Vue.ref(false)
        // Manage dragging state
        const isDragging = Vue.ref(false)
        // Record distance between two touch points
        let lastTouchDistance = 0
        // Safari's gesture events report the scale since the gesture started, not per event
        let lastGestureScale = 1
        // Record mouse position
        let lastMouseX = 0
        let lastMouseY = 0

        // Reject non-finite scale candidates so a single bad input (NaN deltaY, divide-by-zero
        // factor, …) can't poison every subsequent transform with NaN.
        const clampScale = (s) => {
          if (!Number.isFinite(s)) return scale.value
          return Math.min(Math.max(s, MIN_SCALE), MAX_SCALE)
        }

        // Apply zoom by `factor` anchored at the given client coordinates so the world point
        // currently under (clientX, clientY) stays under it after the scale change.
        // The transform is `translate(tx, ty) scale(s)` with origin (0,0): `translate` runs
        // before `scale` (CSS applies transforms right-to-left), so tx/ty are screen pixels
        // relative to the untransformed viewport's top-left.
        const zoomAt = (clientX, clientY, factor) => {
          if (!Number.isFinite(factor) || factor <= 0) return
          if (!Number.isFinite(scale.value) || scale.value <= 0) {
            // Recover from a previously corrupted state instead of silently propagating NaN.
            scale.value = 1
            posX.value = 0
            posY.value = 0
            return
          }
          const newScale = clampScale(scale.value * factor)
          if (!Number.isFinite(newScale) || newScale === scale.value) return
          const container = viewport.value
          if (!container) return
          const rect = container.getBoundingClientRect()
          if (rect.width === 0 || rect.height === 0) return
          const cx = clientX - rect.left
          const cy = clientY - rect.top
          const ratio = newScale / scale.value
          posX.value = cx + (posX.value - cx) * ratio
          posY.value = cy + (posY.value - cy) * ratio
          scale.value = newScale
        }

        const zoomCentered = (factor) => {
          const container = viewport.value
          if (!container) return
          const rect = container.getBoundingClientRect()
          if (rect.width === 0 || rect.height === 0) return
          zoomAt(rect.left + rect.width / 2, rect.top + rect.height / 2, factor)
        }

        const resetView = () => {
          scale.value = 1
          posX.value = 0
          posY.value = 0
        }

        // Calculate distance between two touch points
        const getDistance = (touches) => {
          return Math.hypot(
            touches[0].clientX - touches[1].clientX,
            touches[0].clientY - touches[1].clientY
          )
        }

        // Calculate center point of two touch positions
        const getTouchCenter = (touches) => {
          return {
            x: (touches[0].clientX + touches[1].clientX) / 2,
            y: (touches[0].clientY + touches[1].clientY) / 2
          }
        }

        // Handle touch start
        const handleTouchStart = (e) => {
          if (e.touches.length === 2) {
            e.preventDefault()
            lastTouchDistance = getDistance(e.touches)
          } else {
            // Any non-2-touch state invalidates the baseline — otherwise a stale distance
            // from a previous gesture leaks into the next pinch (e.g. 1→2→3→2 finger
            // transitions on iOS) and produces a discontinuous jump on the first frame.
            lastTouchDistance = 0
          }
        }

        // Handle touch move (pinch in/out for zoom). Uses the ratio of touch-point distances —
        // matches platform pinch gesture conventions (each pinch unit scales by the same factor
        // regardless of starting spread).
        const handleTouchMove = (e) => {
          if (e.touches.length !== 2) {
            // Invalidate the pinch baseline as soon as we leave the 2-finger state, so the next
            // 2-finger frame is treated as a fresh start instead of comparing to a stale distance.
            lastTouchDistance = 0
            return
          }
          // Gate preventDefault on the cursor being over the diagram, so 2-finger gestures on
          // surrounding UI (sidebar, footer) still trigger their default browser behaviour.
          // Use `viewport` (the parent of both the SVG and the snapshot <img>) so snapshot mode
          // keeps working — `#preview` is hidden while the snapshot is on screen.
          if (!viewport.value || !viewport.value.contains(e.target)) return
          e.preventDefault()
          const newDistance = getDistance(e.touches)
          if (lastTouchDistance > 0 && newDistance > 0) {
            const center = getTouchCenter(e.touches)
            zoomAt(center.x, center.y, newDistance / lastTouchDistance)
          }
          lastTouchDistance = newDistance
        }

        // Handle touch end. Any drop below 2 active touches invalidates the pinch baseline so
        // a re-pinch starts fresh.
        const handleTouchEnd = (e) => {
          if (e.touches.length < 2) {
            lastTouchDistance = 0
          }
        }

        // Safari has no Ctrl + wheel pinch: a trackpad pinch arrives as gesturestart /
        // gesturechange / gestureend. Without preventDefault Safari zooms the whole page instead.
        // iOS Safari sends the same gesture events alongside the touch events of a two-finger
        // pinch, which the touch handlers already zoom by, so a pinch in progress is left to them.
        const handleGestureStart = (e) => {
          if (!viewport.value || !viewport.value.contains(e.target)) return
          e.preventDefault()
          lastGestureScale = e.scale || 1
        }

        const handleGestureChange = (e) => {
          if (!viewport.value || !viewport.value.contains(e.target)) return
          e.preventDefault()
          if (lastTouchDistance === 0 && lastGestureScale > 0 && e.scale > 0) {
            zoomAt(e.clientX, e.clientY, e.scale / lastGestureScale)
          }
          lastGestureScale = e.scale
        }

        const handleGestureEnd = (e) => {
          if (!viewport.value || !viewport.value.contains(e.target)) return
          e.preventDefault()
          lastGestureScale = 1
        }

        // Keys typed into these belong to the control, not to the diagram. Checkboxes only
        // react to Space, so zoom keys still reach the diagram from the model list.
        const isTypingTarget = (element) => {
          if (!element) return false
          if (element.isContentEditable) return true
          if (element.tagName === 'TEXTAREA' || element.tagName === 'SELECT') return true
          return element.tagName === 'INPUT' && element.type !== 'checkbox'
        }

        // Arrow keys scroll the model list while the user is working in the sidebar (last clicked
        // or tabbed into), and move the diagram otherwise.
        let isSidebarActive = false
        const ARROW_KEYS = ['ArrowUp', 'ArrowDown', 'ArrowLeft', 'ArrowRight']

        const KEY_ACTIONS = {
          ArrowUp: () => moveUp(),
          ArrowDown: () => moveDown(),
          ArrowLeft: () => moveLeft(),
          ArrowRight: () => moveRight(),
          '+': () => zoomIn(),
          // `=` shares the + key on US layouts, so zooming in doesn't need Shift
          '=': () => zoomIn(),
          '-': () => zoomOut(),
          '0': () => resetView(),
        }

        // Handle key down: space toggles drag mode; arrows move, + / - zoom and 0 resets the view
        const handleKeyDown = (e) => {
          if (e.code === 'Space' && !isSpacePressed.value) {
            if (document.activeElement.tagName === 'INPUT' || document.activeElement.tagName === 'TEXTAREA') {
              return
            }
            isSpacePressed.value = true
            e.preventDefault()
            return
          }
          const action = KEY_ACTIONS[e.key]
          // Ctrl / Command / Alt combinations are the browser's (page zoom, history navigation)
          if (!action || tab.value !== 'erd' || e.ctrlKey || e.metaKey || e.altKey) return
          if (isTypingTarget(document.activeElement)) return
          if (ARROW_KEYS.includes(e.key) && (isSidebarActive || (sidebar.value && sidebar.value.contains(document.activeElement)))) return
          e.preventDefault()
          action()
        }

        // Handle key up
        const handleKeyUp = (e) => {
          if (e.code === 'Space') {
            isSpacePressed.value = false
            isDragging.value = false
          }
        }

        // Handle mouse down (start dragging)
        const handleMouseDown = (e) => {
          isSidebarActive = !!(sidebar.value && sidebar.value.contains(e.target))
          if (isSpacePressed.value || e.button === 1) {
            isDragging.value = true
            lastMouseX = e.clientX
            lastMouseY = e.clientY
            e.preventDefault()
          }
        }

        // Handle mouse move (movement while dragging). Translates are in screen pixels because
        // `translate` runs before `scale` in the transform (CSS applies transforms right-to-left),
        // so the scale factor never multiplies the translate — no compensation needed.
        const handleMouseMove = (e) => {
          if (isDragging.value) {
            posX.value += e.clientX - lastMouseX
            posY.value += e.clientY - lastMouseY
            lastMouseX = e.clientX
            lastMouseY = e.clientY
            e.preventDefault()
          }
        }

        // Handle mouse up (end dragging)
        const handleMouseUp = (e) => {
          if (e.button === 1) {
            e.preventDefault()
          }
          isDragging.value = false
        }

        // Handle wheel: scrolling (a mouse wheel or a two-finger trackpad swipe) moves the diagram;
        // Ctrl / Command + scrolling and trackpad pinches (which browsers send as Ctrl + wheel)
        // zoom it, anchored at the cursor.
        const handleWheel = (e) => {
          // Use `viewport` rather than `#preview` so snapshot mode (which swaps `#preview` for
          // an <img> sibling under the same viewport) keeps responding to the wheel.
          if (!viewport.value || !viewport.value.contains(e.target)) return
          if (e.deltaX === 0 && e.deltaY === 0) return
          e.preventDefault()
          // Normalise non-pixel deltaMode (Firefox defaults to line mode). Page mode is rare;
          // scale it against the diagram area itself rather than the whole document.
          const toPixels = (delta, pagePx) => e.deltaMode === 1
            ? delta * WHEEL_LINE_HEIGHT_PX
            : e.deltaMode === 2
              ? delta * pagePx
              : delta
          const pixelDeltaX = toPixels(e.deltaX, viewport.value.clientWidth)
          const pixelDeltaY = toPixels(e.deltaY, viewport.value.clientHeight)
          if (e.ctrlKey || e.metaKey) {
            const zoomDelta = Math.min(Math.max(pixelDeltaY, -WHEEL_ZOOM_MAX_DELTA_PX), WHEEL_ZOOM_MAX_DELTA_PX)
            zoomAt(e.clientX, e.clientY, Math.exp(-zoomDelta * WHEEL_ZOOM_SENSITIVITY))
            return
          }
          // Browsers that don't turn Shift + mouse wheel into a horizontal scroll still get one
          if (e.shiftKey && pixelDeltaX === 0) {
            move(-pixelDeltaY, 0)
            return
          }
          move(-pixelDeltaX, -pixelDeltaY)
        }

        const zoomStyle = Vue.computed(() => {
          return {
            transform: `translate(${posX.value}px, ${posY.value}px) scale(${scale.value})`,
            transformOrigin: '0 0'
          }
        })

        const zoomIn = () => zoomCentered(BUTTON_ZOOM_FACTOR)
        const zoomOut = () => zoomCentered(1 / BUTTON_ZOOM_FACTOR)

        const move = (dxPx, dyPx) => {
          posX.value += dxPx
          posY.value += dyPx
        }

        const moveUp = () => move(0, -PAN_STEP_PX)
        const moveDown = () => move(0, PAN_STEP_PX)
        const moveLeft = () => move(-PAN_STEP_PX, 0)
        const moveRight = () => move(PAN_STEP_PX, 0)

        const restoreFromHash = () => {
          // A completely empty hash is the normal "first visit" state, not a
          // parse failure — silently fall through to `reset()`. Anything else
          // that fails to parse is likely a shared link the user copy-pasted
          // and now can't restore from; surface a warning so they have a
          // breadcrumb instead of just "my settings disappeared."
          const raw = location.hash.substr(1)
          if (!raw) {
            reset()
            return
          }
          try {
            const parsedData = JSON.parse(atob(raw))
            selectModels.value = parsedData.selectModels || [...viewerDefaults.models]
            isPreviewRelations.value = !!parsedData.isPreviewRelations
            isShowRelationComment.value = !!parsedData.isShowRelationComment
            isShowKey.value = !!parsedData.isShowKey
            isShowComment.value = !!parsedData.isShowComment
            isShowTableComment.value = !!parsedData.isShowTableComment
            // Links shared before the column choice existed carry the old
            // `isHideColumns` checkbox instead. A link that names neither
            // leaves the choice to the app's default.
            const legacyColumns = parsedData.isHideColumns === undefined
              ? viewerDefaults.columns
              : (parsedData.isHideColumns ? 'none' : 'all')
            columnMode.value = COLUMN_MODES.includes(parsedData.columns) ? parsedData.columns : legacyColumns
            isSnapshotMode.value = !!parsedData.isSnapshotMode
          } catch (err) {
            console.warn('mermaid_erd: could not restore from URL hash, resetting to defaults.', err)
            reset()
          }
        }

        // Default to NO models selected unless the app's config lists some.
        // Issue #169: on schemas with hundreds of models, rendering the full
        // diagram up front made Safari/Firefox crawl — and most users only want
        // a focused slice anyway. Users can still bulk-select via the "All"
        // button in the sidebar.
        const reset = () => {
          selectModels.value = [...viewerDefaults.models]
          isPreviewRelations.value = false
          isShowRelationComment.value = false
          isShowKey.value = false
          isShowComment.value = false
          isShowTableComment.value = false
          columnMode.value = viewerDefaults.columns
          isSnapshotMode.value = false
          updateHash()
        }

        const updateHash = () => {
          location.hash = btoa(JSON.stringify({
            selectModels: selectModels.value,
            isPreviewRelations: isPreviewRelations.value,
            isShowRelationComment: isShowRelationComment.value,
            isShowKey: isShowKey.value,
            isShowComment: isShowComment.value,
            isShowTableComment: isShowTableComment.value,
            columns: columnMode.value,
            isSnapshotMode: isSnapshotMode.value,
          }))
        }

        const onChange = (e) => {
          if (e.target.checked) {
            selectModels.value.push(e.target.value)
          } else {
            selectModels.value = selectModels.value.filter((selectModel) => selectModel !== e.target.value)
          }
          selectModels.value
          updateHash()
        }

        const onSelectAll = () => {
          filteredModelList.value.forEach((model) => {
            if (!selectModels.value.includes(model.ModelName)) {
              selectModels.value.push(model.ModelName)
            }
          })
          updateHash()
        }

        const onUnselectAll = () => {
          selectModels.value = selectModels.value.filter((model) => {
            return !filteredModelList.value.some((fm) => fm.ModelName === model)
          })
          updateHash()
        }

        isCopiedUrl = Vue.ref(false)
        // Clipboard writes can reject — Safari without a user gesture, insecure
        // contexts, sandboxed iframes. Treat the rejection as a real failure
        // instead of silently flashing "Copied" while nothing actually landed.
        const clipboardWrite = (text, copiedRef) => {
          const flashCopied = () => {
            copiedRef.value = true
            setTimeout(() => { copiedRef.value = false }, 1000)
          }
          const writePromise = navigator.clipboard && navigator.clipboard.writeText
            ? navigator.clipboard.writeText(text)
            : Promise.reject(new Error('navigator.clipboard.writeText unavailable'))
          writePromise.then(flashCopied).catch((err) => {
            console.error('mermaid_erd: clipboard write failed', err)
            renderError.value = i18n[language.value]['errors']['copy_failed']
          })
        }
        const onCopyUrl = () => clipboardWrite(location.href, isCopiedUrl)

        const mermaidErd = Vue.computed(() => {
          // Comment metadata is arbitrary DB text. A newline would split a
          // label or terminate a `%%` line mid-diagram, and a literal `"`
          // closes a quoted label early — so collapse newlines to a space and
          // escape `"` as Mermaid's `#quot;` entity. Kept in lockstep with the
          // Ruby renderer (lib/rails-mermaid_erd/mermaid_text.rb).
          const oneLine = (v) => String(v ?? '').replace(/[\r\n]+/g, ' ')
          const quoted = (v) => `"${oneLine(v).replace(/"/g, '#quot;')}"`

          const lines = []
          lines.push('erDiagram')
          lines.push('    %% --------------------------------------------------------')
          lines.push('    %% Generated by "Rails Mermaid ERD"')
          lines.push('    %% https://github.com/koedame/rails-mermaid_erd')
          lines.push(`    %% Restore Hash: ${location.hash}`)
          lines.push('    %% --------------------------------------------------------')
          lines.push('')

          filteredData.value.Models.forEach(model => {
            lines.push(`    %% table name: ${oneLine(model.TableName)}`)
            lines.push(`    %% table comment: ${oneLine(model.TableComment)}`)
            const entityName = model.ModelName.replace(/:/g, '-')
            // Mermaid's entity alias: the id stays the model name (relations
            // refer to it), the heading shows "Model / table comment".
            const entityAlias = isShowTableComment.value && oneLine(model.TableComment) !== ''
              ? `["${entityName} / ${oneLine(model.TableComment).replace(/"/g, '#quot;')}"]`
              : ''
            lines.push(`    ${entityName}${entityAlias} {`)

            if (columnMode.value !== 'none') {
              model.Columns.filter(column => columnMode.value === 'all' || column.key).forEach(column => {
                lines.push(`        ${column.type} ${column.name} ${isShowKey.value ? column.key : ''} ${isShowComment.value ? quoted(column.comment) : ''}`)
              })
            }

            lines.push(`    }`)
            lines.push('')
          });

          filteredData.value.Relations.forEach(relation => {
            if (isShowRelationComment.value) {
              lines.push(`    ${relation.LeftModelName.replace(/:/g, '-')} ${relation.LeftValue}${relation.Line}${relation.RightValue} ${relation.RightModelName.replace(/:/g, '-')} : ${quoted(relation.Comment)}`)
            } else {
              lines.push(`    ${relation.LeftModelName.replace(/:/g, '-')} ${relation.LeftValue}${relation.Line}${relation.RightValue} ${relation.RightModelName.replace(/:/g, '-')} : ""`)
            }
          });

          return lines.join("\n")
        })

        const filteredData = Vue.computed(() => {
          let relations
          if (isPreviewRelations.value) {
            relations = schemaData.Relations.filter(relation => {
              return selectModels.value.includes(relation.LeftModelName) || selectModels.value.includes(relation.RightModelName)
            })
          } else {
            relations = schemaData.Relations.filter(relation => {
              return selectModels.value.includes(relation.LeftModelName) && selectModels.value.includes(relation.RightModelName)
            })
          }

          return {
            Models: schemaData.Models.filter(model => selectModels.value.includes(model.ModelName)),
            Relations: relations
          }
        })

        const graph = Vue.ref()

        const filterText = Vue.ref('')

        // Mermaid initialise is non-trivial — it parses theme CSS and seeds an
        // internal config. Doing it on every reRender (the previous behaviour)
        // wasted work on schemas that re-render frequently. Init once here.
        // `securityLevel: 'strict'` is also the Mermaid 10+ default, but pin it
        // explicitly so a future upstream change can't silently regress to
        // 'loose' (which would eval user-supplied labels as HTML).
        mermaid.initialize({
          theme: 'dark',
          maxTextSize: 99999999,
          startOnLoad: false,
          securityLevel: 'strict',
          flowchart: { htmlLabels: false },
          er: { useMaxWidth: false }
        })

        // Convenience: do `selectModels.length === 0` once. The reRender
        // short-circuit and the empty-state overlay (template line ~258) both
        // gate on this, so any future change to "what counts as empty?" only
        // needs to update one place.
        const isEmptySelection = Vue.computed(() => filteredData.value.Models.length === 0)

        // The set of refs that actually affect the rendered diagram. Toggling
        // `isSnapshotMode` does NOT belong here — when the user flips snapshot
        // on/off we want to skip re-parsing Mermaid and just re-rasterise the
        // existing SVG.
        const computeRenderKey = () => JSON.stringify([
          [...selectModels.value].sort(),
          isPreviewRelations.value,
          isShowRelationComment.value,
          isShowKey.value,
          isShowComment.value,
          isShowTableComment.value,
          columnMode.value
        ])
        let lastRenderKey = null

        // Track the in-flight render so a late-completing render can't
        // overwrite a more recent one when the user toggles options rapidly.
        let renderToken = 0
        const reRender = async () => {
          const myToken = ++renderToken
          const source = mermaidErd.value
          const key = computeRenderKey()
          const renderAffectingChanged = key !== lastRenderKey

          // Empty selection short-circuits Mermaid (it would render a header-
          // only diagram, but skipping the call avoids the parser cost on big
          // schemas where the user just landed on an empty default view).
          if (isEmptySelection.value) {
            graph.value = ''
            snapshotDataUrl.value = ''
            renderError.value = ''
            lastRenderKey = key
            const target = document.getElementById('preview')
            if (target) target.innerHTML = ''
            return
          }

          // Snapshot-only toggles re-bake the existing SVG without re-parsing.
          // This is the single biggest reason snapshot mode feels instant on
          // schemas where mermaid.render itself takes seconds. We only take
          // this branch when the last render actually succeeded (graph.value
          // populated) — otherwise we fall through and retry mermaid.render.
          if (!renderAffectingChanged && graph.value) {
            await syncSnapshot(myToken)
            return
          }

          let svg
          try {
            const result = await mermaid.render('mermaid-erd', source)
            svg = result.svg
          } catch (err) {
            // Deliberately do NOT update lastRenderKey on failure — otherwise
            // the next attempt with the same selection would short-circuit
            // and the user would be unable to retry without changing options.
            console.error('mermaid_erd: failed to render diagram', err)
            renderError.value = i18n[language.value]['errors']['render_failed']
            return
          }
          if (myToken !== renderToken) return
          graph.value = svg
          renderError.value = ''
          lastRenderKey = key
          const target = document.getElementById('preview')
          if (!target) return
          target.innerHTML = svg
          if (isSnapshotMode.value) {
            await bakeSnapshot(target, myToken)
          } else {
            snapshotDataUrl.value = ''
          }
        }

        // Sync the snapshot image to whatever's currently in `graph.value`,
        // without going back through Mermaid.
        const syncSnapshot = async (myToken) => {
          if (isSnapshotMode.value) {
            const target = document.getElementById('preview')
            if (target && target.querySelector('svg')) {
              await bakeSnapshot(target, myToken)
            }
          } else {
            snapshotDataUrl.value = ''
          }
        }

        // Encode UTF-8 text to base64 without the deprecated `unescape` pattern.
        // Returns null on failure so callers can surface the error explicitly —
        // a thrown btoa error inside an img.onload handler would hang the
        // surrounding Promise forever.
        const utf8ToBase64 = (str) => {
          try {
            const bytes = new TextEncoder().encode(str)
            let binary = ''
            for (let i = 0; i < bytes.length; i++) binary += String.fromCharCode(bytes[i])
            return btoa(binary)
          } catch (err) {
            console.error('mermaid_erd: utf8ToBase64 failed', err)
            return null
          }
        }

        // Rasterise the rendered SVG into a PNG data URL and swap it into the
        // preview element. Pan/zoom now transforms a single bitmap instead of
        // a deep SVG tree — the biggest single win on Safari/Firefox for
        // hundreds-of-models schemas (#169).
        //
        // On any failure we set `isSnapshotMode = false` so the toggle in the
        // sidebar stays in sync with what the user actually sees (otherwise
        // the SVG silently reappears while the checkbox stays ticked — exactly
        // the "fallback masks the real problem" pattern called out in review).
        const bakeSnapshot = (target, myToken) => {
          return new Promise((resolve) => {
            // Token-guarded failure surface. A stale render's late-arriving
            // img.onerror used to clobber the *current* render's UI state
            // (renderError, isSnapshotMode); the token check at the top keeps
            // late failures from leaking into a fresher cycle.
            const fail = (reason) => {
              if (myToken !== renderToken) {
                resolve()
                return
              }
              console.error(`mermaid_erd: snapshot bake failed (${reason})`)
              snapshotDataUrl.value = ''
              isSnapshotMode.value = false
              renderError.value = i18n[language.value]['errors']['snapshot_failed']
              resolve()
            }
            const svgNode = target.querySelector('svg')
            if (!svgNode) return fail('no SVG node in preview')
            const width = svgNode.viewBox?.baseVal?.width || svgNode.width?.baseVal?.value || svgNode.getBoundingClientRect().width
            const height = svgNode.viewBox?.baseVal?.height || svgNode.height?.baseVal?.value || svgNode.getBoundingClientRect().height
            if (!width || !height) return fail('SVG has no dimensions')
            const svgData = new XMLSerializer().serializeToString(svgNode)
            const svgBase64 = utf8ToBase64(svgData)
            if (svgBase64 === null) return fail('failed to base64-encode SVG')
            const img = new Image()
            img.onload = () => {
              if (myToken !== renderToken || !isSnapshotMode.value) {
                resolve()
                return
              }
              // Single try/catch covers drawImage AND toDataURL: a synchronous
              // throw from either inside img.onload would leave this Promise
              // pending forever, hanging the calling reRender's await.
              try {
                const canvas = document.createElement('canvas')
                // Cap the bitmap at a sane max so very large diagrams don't blow
                // out GPU texture limits (~16k px on most browsers). We still
                // preserve aspect ratio so layout matches the SVG.
                const MAX_DIMENSION = 8192
                const ratio = Math.min(1, MAX_DIMENSION / Math.max(width, height))
                canvas.width = Math.max(1, Math.floor(width * ratio))
                canvas.height = Math.max(1, Math.floor(height * ratio))
                const ctx = canvas.getContext('2d')
                ctx.drawImage(img, 0, 0, canvas.width, canvas.height)
                snapshotDataUrl.value = canvas.toDataURL('image/png')
                renderError.value = ''
              } catch (err) {
                return fail(`canvas operation failed: ${err.message}`)
              }
              resolve()
            }
            img.onerror = () => fail('Image failed to load rasterised SVG')
            img.src = 'data:image/svg+xml;charset=utf-8;base64,' + svgBase64
          })
        }

        // Debounce reRender so rapid toggles (e.g. clicking "All", or scrubbing
        // option checkboxes) don't issue back-to-back mermaid.render calls —
        // each render is O(models × relations) in the parser.
        let reRenderTimer = null
        const RE_RENDER_DEBOUNCE_MS = 100
        const scheduleReRender = () => {
          if (reRenderTimer) clearTimeout(reRenderTimer)
          reRenderTimer = setTimeout(() => {
            reRenderTimer = null
            reRender()
          }, RE_RENDER_DEBOUNCE_MS)
        }

        const onCopyMermaid = () => clipboardWrite(mermaidErd.value, isCopiedMermaid)

        const onCopyMarkdown = () => clipboardWrite(`\`\`\`mermaid\n${mermaidErd.value}\n\`\`\`\n`, isCopiedMarkdown)

        // Surface download failures (empty selection, post-error state) via the
        // same `renderError` banner the render path uses — silently producing a
        // 0-byte file used to look like a broken Download button.
        const failDownload = (reason, err) => {
          if (err) console.error(`mermaid_erd: download failed (${reason})`, err)
          else console.error(`mermaid_erd: download failed (${reason})`)
          renderError.value = i18n[language.value]['errors']['download_failed']
        }

        const onDownloadSvg = () => {
          if (!graph.value) return failDownload('no rendered SVG to export')
          try {
            const svgBlob = new Blob([graph.value], { type: 'image/svg+xml;charset=utf-8' })
            const svgUrl = URL.createObjectURL(svgBlob)
            const a = document.createElement('a')
            a.href = svgUrl
            a.setAttribute('download', 'mermaid_erd.svg')
            a.dispatchEvent(new MouseEvent('click'))
            URL.revokeObjectURL(svgUrl)
          } catch (err) {
            failDownload('SVG export threw', err)
          }
        }

        const onDownloadPng = () => {
          const svg = document.querySelector('#preview > svg')
          if (!svg) return failDownload('no rendered SVG to export')
          let svgBase64, width, height
          try {
            const svgData = new XMLSerializer().serializeToString(svg)
            svgBase64 = utf8ToBase64(svgData)
            if (svgBase64 === null) return failDownload('SVG base64 encoding failed')
            width = svg.width.baseVal.value
            height = svg.height.baseVal.value
            if (!width || !height) return failDownload('SVG has no dimensions')
          } catch (err) {
            return failDownload('PNG export setup threw', err)
          }
          const canvas = document.createElement('canvas')
          canvas.width = width
          canvas.height = height
          const ctx = canvas.getContext('2d')
          const image = new Image
          image.onload = () => {
            try {
              ctx.drawImage(image, 0, 0)
              const a = document.createElement('a')
              a.href = canvas.toDataURL('image/png')
              a.setAttribute('download', 'mermaid_erd.png')
              a.dispatchEvent(new MouseEvent('click'))
            } catch (err) {
              failDownload('canvas operation threw', err)
            }
          }
          image.onerror = () => failDownload('Image failed to load rasterised SVG')
          image.src = 'data:image/svg+xml;charset=utf-8;base64,' + svgBase64
        }

        const filteredModelList = Vue.computed(() => {
          return schemaData.Models.filter((model) => {
            if (model.ModelName.toLowerCase().includes(filterText.value.toLowerCase())) {
              return true
            }
            if (model.TableName.toLowerCase().includes(filterText.value.toLowerCase())) {
              return true
            }
          })
        })

        const isExistsNoModelTable = Vue.computed(() => {
          return filteredModelList.value.some((model) => !model.IsModelExist)
        })

        // Sidebar virtualisation.
        //
        // For schemas with hundreds of models the sidebar's v-for was creating a
        // <label> + <input> + (sometimes) <svg> per row. The browser then paid
        // layout cost for every row whether it was on screen or not. The fix is
        // a windowed list: render only the rows currently inside the scroll
        // viewport, plus a small buffer.
        //
        // Item height has to be a constant for the maths to work — the row
        // labels in the template bind their height to it, so resist adding
        // wrap/multi-line content.
        //
        // The list has no scroll box of its own: it scrolls with the sidebar,
        // so the window is derived from how far the sidebar has scrolled past
        // the top of the list, and sized by the sidebar's height, which follows
        // the browser window and is therefore measured rather than assumed.
        const VIRTUAL_LIST_ITEM_HEIGHT_PX = 24
        const VIRTUAL_LIST_BUFFER_ROWS = 8
        const VIRTUAL_LIST_THRESHOLD = 80
        const sidebar = Vue.ref(null)
        const modelListHeader = Vue.ref(null)
        const virtualList = Vue.ref(null)
        const virtualListScrollTop = Vue.ref(0)
        const virtualListClientHeight = Vue.ref(0)
        const modelListHeaderHeight = Vue.ref(0)
        const sidebarResizeObserver = new ResizeObserver((entries) => {
          for (const entry of entries) {
            if (entry.target === sidebar.value) {
              virtualListClientHeight.value = entry.target.clientHeight
            } else {
              modelListHeaderHeight.value = entry.target.offsetHeight
            }
          }
        })

        const isVirtualizingModels = Vue.computed(() => filteredModelList.value.length > VIRTUAL_LIST_THRESHOLD)

        const virtualListVisibleRange = Vue.computed(() => {
          const total = filteredModelList.value.length
          if (!isVirtualizingModels.value) {
            return { start: 0, end: total }
          }
          const visibleCount = Math.ceil(virtualListClientHeight.value / VIRTUAL_LIST_ITEM_HEIGHT_PX)
          const start = Math.max(0, Math.floor(virtualListScrollTop.value / VIRTUAL_LIST_ITEM_HEIGHT_PX) - VIRTUAL_LIST_BUFFER_ROWS)
          const end = Math.min(total, start + visibleCount + VIRTUAL_LIST_BUFFER_ROWS * 2)
          return { start, end }
        })

        const virtualListVisibleModels = Vue.computed(() => {
          const { start, end } = virtualListVisibleRange.value
          return filteredModelList.value.slice(start, end)
        })

        const virtualListTopSpacerStyle = Vue.computed(() => {
          if (!isVirtualizingModels.value) return { display: 'none' }
          return { height: `${virtualListVisibleRange.value.start * VIRTUAL_LIST_ITEM_HEIGHT_PX}px` }
        })

        const virtualListBottomSpacerStyle = Vue.computed(() => {
          if (!isVirtualizingModels.value) return { display: 'none' }
          const total = filteredModelList.value.length
          const trailing = total - virtualListVisibleRange.value.end
          return { height: `${Math.max(0, trailing) * VIRTUAL_LIST_ITEM_HEIGHT_PX}px` }
        })

        const onSidebarScroll = () => {
          const listTop = virtualList.value.getBoundingClientRect().top - sidebar.value.getBoundingClientRect().top
          virtualListScrollTop.value = Math.max(0, -listTop)
        }

        // Filtering shrinks the list — if it had been scrolled under the pinned
        // header, scroll back so its first row sits right below the header
        // (otherwise the window could sit past the new end and render no rows
        // above a tall empty spacer). The header holds the filter input, so it
        // stays pinned while the user types, unless the filtered list is too
        // short to keep the sidebar scrolled that far. Runs after the DOM update
        // so the positions describe the filtered list. The sidebar opts out of
        // scroll anchoring: otherwise Chromium restores an older scroll position
        // when the list grows back, and this would scroll to the header.
        Vue.watch(filterText, () => {
          const hidden = modelListHeader.value.getBoundingClientRect().bottom - virtualList.value.getBoundingClientRect().top
          if (hidden > 0) sidebar.value.scrollTop -= hidden
          onSidebarScroll()
        }, { flush: 'post' })

        const resolveLocale = (raw) => {
          if (!raw) return 'en'
          const codes = window.locales.map((l) => l.code)
          const exact = codes.find((c) => c.toLowerCase() === raw.toLowerCase())
          if (exact) return exact
          const base = raw.toLowerCase().split('-')[0]
          // zh is the only language that ships two variants, so route it by script/region;
          // every other region variant (fr-FR, pt-PT, ...) falls back to its base language.
          if (base === 'zh') return /hant|tw|hk|mo/.test(raw.toLowerCase()) ? 'zh-TW' : 'zh-CN'
          const baseMatch = codes.find((c) => c.toLowerCase().split('-')[0] === base)
          return baseMatch || 'en'
        }

        const setLanguage = (l) => {
          const resolved = resolveLocale(l)
          language.value = resolved
          const meta = window.locales.find((x) => x.code === resolved)
          document.documentElement.lang = resolved
          document.documentElement.dir = (meta && meta.dir) || 'ltr'
        }

        Vue.onMounted(() => {
          setLanguage(window.navigator.language)
          restoreFromHash()
          reRender()
          // After first render, any subsequent option/selection change is
          // pushed through the hashchange path, which is debounced.

          window.addEventListener('keydown', handleKeyDown)
          window.addEventListener('keyup', handleKeyUp)
          window.addEventListener('mousedown', handleMouseDown)
          window.addEventListener('mousemove', handleMouseMove)
          window.addEventListener('mouseup', handleMouseUp)
          window.addEventListener('wheel', handleWheel, { passive: false })
          window.addEventListener('touchstart', handleTouchStart, { passive: false })
          window.addEventListener('touchmove', handleTouchMove, { passive: false })
          window.addEventListener('touchend', handleTouchEnd)
          window.addEventListener('gesturestart', handleGestureStart, { passive: false })
          window.addEventListener('gesturechange', handleGestureChange, { passive: false })
          window.addEventListener('gestureend', handleGestureEnd, { passive: false })
          sidebarResizeObserver.observe(sidebar.value)
          sidebarResizeObserver.observe(modelListHeader.value)
        })

        Vue.onUnmounted(() => {
          window.removeEventListener('keydown', handleKeyDown)
          window.removeEventListener('keyup', handleKeyUp)
          window.removeEventListener('mousedown', handleMouseDown)
          window.removeEventListener('mousemove', handleMouseMove)
          window.removeEventListener('mouseup', handleMouseUp)
          window.removeEventListener('wheel', handleWheel)
          window.removeEventListener('touchstart', handleTouchStart)
          window.removeEventListener('touchmove', handleTouchMove)
          window.removeEventListener('touchend', handleTouchEnd)
          window.removeEventListener('gesturestart', handleGestureStart)
          window.removeEventListener('gesturechange', handleGestureChange)
          window.removeEventListener('gestureend', handleGestureEnd)
          sidebarResizeObserver.disconnect()
        })

        window.addEventListener('hashchange', () => {
          restoreFromHash()
          scheduleReRender()
        }, false);

        return {
          filteredModelList,
          filterText,
          i18n: window.i18n,
          isCopiedMarkdown,
          isCopiedMermaid,
          isCopiedUrl,
          isExistsNoModelTable,
          isPreviewRelations,
          isShowRelationComment,
          isShowComment,
          isShowTableComment,
          isShowKey,
          isSnapshotMode,
          snapshotDataUrl,
          isEmptySelection,
          renderError,
          language,
          locales: window.locales,
          mermaidErd,
          onChange,
          onCopyMarkdown,
          onCopyMermaid,
          onCopyUrl,
          onDownloadPng,
          onDownloadSvg,
          onSelectAll,
          onUnselectAll,
          reset,
          schemaData,
          selectModels,
          setLanguage,
          tab,
          updateHash,
          COLUMN_MODES,
          columnMode,
          zoomStyle,
          zoomArea,
          viewport,
          zoomIn,
          zoomOut,
          resetView,
          moveUp,
          moveDown,
          moveLeft,
          moveRight,
          isSpacePressed,
          isDragging,
          sidebar,
          modelListHeader,
          modelListHeaderHeight,
          VIRTUAL_LIST_ITEM_HEIGHT_PX,
          virtualList,
          onSidebarScroll,
          virtualListTopSpacerStyle,
          virtualListBottomSpacerStyle,
          virtualListVisibleModels
        }
      }
    }

    Vue.createApp(App).mount('#app')
  </script>
</body>
</html>
